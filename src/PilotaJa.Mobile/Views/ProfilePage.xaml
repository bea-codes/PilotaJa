<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PilotaJa.Mobile.ViewModels"
             x:Class="PilotaJa.Mobile.Views.ProfilePage"
             x:DataType="vm:ProfileViewModel"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource Background}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            
            <!-- Avatar -->
            <Frame WidthRequest="100" HeightRequest="100" CornerRadius="50" 
                   BackgroundColor="#E5E7EB" Padding="0" HorizontalOptions="Center" Margin="0,20,0,0">
                <Label Text="👤" FontSize="40" VerticalOptions="Center" HorizontalOptions="Center"/>
            </Frame>

            <!-- User Info (if logged in) -->
            <VerticalStackLayout Spacing="8" IsVisible="{Binding IsLoggedIn}">
                <Label Text="{Binding Profile.Name}" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center"/>
                <Label Text="{Binding Profile.Email}" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center"/>
                <Label Text="{Binding Profile.Phone}" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <!-- Not logged in -->
            <VerticalStackLayout Spacing="16" IsVisible="{Binding IsLoggedIn, Converter={StaticResource InvertedBoolConverter}}">
                <Label Text="Você não está logado" FontSize="18" HorizontalOptions="Center"/>
                <Button Text="Entrar" Style="{StaticResource PrimaryButton}"/>
                <Button Text="Criar Conta" BackgroundColor="Transparent" TextColor="{StaticResource Primary}"/>
            </VerticalStackLayout>

            <!-- Menu -->
            <Frame BackgroundColor="{StaticResource Surface}" CornerRadius="12" Padding="0" Margin="0,20,0,0">
                <VerticalStackLayout>
                    <Button Text="📝 Meus Dados" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Fill"/>
                    <BoxView HeightRequest="1" Color="#E5E7EB"/>
                    <Button Text="🔔 Notificações" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Fill"/>
                    <BoxView HeightRequest="1" Color="#E5E7EB"/>
                    <Button Text="❓ Ajuda" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Fill"/>
                    <BoxView HeightRequest="1" Color="#E5E7EB"/>
                    <Button Text="📄 Termos de Uso" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Fill"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Logout -->
            <Button Text="Sair" 
                    BackgroundColor="#EF4444" 
                    TextColor="White"
                    CornerRadius="8"
                    Command="{Binding LogoutCommand}"
                    IsVisible="{Binding IsLoggedIn}"
                    Margin="0,20,0,0"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
